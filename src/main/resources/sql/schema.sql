-- To be specified in future, for now relying on hibernate schema generation.

--CREATE TABLE reservation (
--  id BIGINT NOT NULL,
--  name VARCHAR(255) NOT NULL,
--  date date NOT NULL,
--  time time NOT NULL,
--  people INT NOT NULL,
--  CONSTRAINT pk_reservation PRIMARY KEY (id)
--);